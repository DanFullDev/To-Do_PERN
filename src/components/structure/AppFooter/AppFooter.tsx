import "./AppFooter.css";

export default function AppFooter() {
  return (
    <div
      className="font-semibold text-center text-gray-500 mt-5 
      text-opacity-40 text-xl transform duration-500
      ease-in-out hover:scale-110 cursor-pointer"
    >
      Drag and drop to sort list
    </div>
  );
}
