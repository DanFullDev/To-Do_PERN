const pool = require("../db");

const Todo = {
  loadAll() {},
  loadOne(id) {},
  editOne(description, status, id) {},
  createOne(description, status) {},
  deleteOne(id) {},
};

module.exports = Todo;
